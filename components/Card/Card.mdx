import { Canvas, Meta, Controls } from '@storybook/blocks';
import * as CardStories from './Card.stories.tsx';
 
<Meta of={CardStories} />
 
# Card
 
TODO: Documentation for the `<Card />` component.

<Controls of={CardStories._Card} />
 
<Canvas of={CardStories._Card} />
